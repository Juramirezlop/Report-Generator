Only one external library required for execution of main code, for grphic interface used

python -m pip install -U pip python -m pip install -U matplotlib
python -m pip install -U pip python -m pip install -U tkinter
tkinter == 3.9.1
matplotlib == 3.3.3
